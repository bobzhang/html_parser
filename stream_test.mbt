// Public regression tests for the streaming API have been moved into the
// `stream/` package as blackbox snapshot tests:
//
//   - stream/stream_events_test.mbt   — `stream` / `stream_each` events
//   - stream/stream_bytes_test.mbt    — `stream_bytes` / `stream_bytes_each`
//   - stream/stream_sink_test.mbt     — `StreamSink` building block
//
// Doubles as runnable documentation; see `stream/README.mbt.md`.
